rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all authenticated users (for testing/seeding)
    // TODO: Restore role-based rules after Cloud Functions deployed
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

export async function seedDemoData() {
  console.log('🌱 Seed data: Firebase removed, use Supabase dashboard to seed data.');
  return {
    success: true,
    message: 'Seed function is a placeholder. Firebase has been removed from this project.'
  };
}
